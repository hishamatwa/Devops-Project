# Kubernetes Files

This folder contains the Kubernetes YAML files for the Voting App.

## What is inside
- `namespace.yaml`  
  Creates the namespace `voting-app`.

- `app/`  
  Contains the Deployments and Services for:
  db, redis, worker, vote, and result.
